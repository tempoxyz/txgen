# Tempo TIP20 workload using native 1-of-1 multisig sender accounts.
#
# `txgen addresses` emits the derived multisig account addresses, not the owner
# signer addresses. Fund those addresses before running the workload.

chain_id: 1337

gas:
  max_fee_per_gas: 100000000000
  max_priority_fee_per_gas: 100000000000

accounts:
  multisigs:
    mnemonic: "test test test test test test test test test test test junk"
    range: [0, ${TXGEN_ACCOUNTS}]
    native_multisig_1_of_1:
      auto_setup: true
      setup_gas_limit: 300000
      setup_fee_token: "0x20c0000000000000000000000000000000000000"

artifacts:
  ERC20: erc20.abi.json

templates:
  tip20_transfer:
    type: tempo
    from:
      pool: multisigs
      select: random
    gas_limit: 300000
    max_fee_per_gas: 100000000000
    max_priority_fee_per_gas: 100000000000
    fee_token: "0x20c0000000000000000000000000000000000000"
    expiring_nonce: true
    valid_for_secs: 10
    call:
      to: "0x20c0000000000000000000000000000000000000"
      abi: ERC20
      function: transfer
      args:
        - pool:
            pool: multisigs
            select: random
        - 1

mix:
  - template: tip20_transfer
    weight: 100
